import cv2

cap = cv2.VideoCapture("rtsp://admin:ulkfyvxl9d@192.168.0.117:554/onvif2")

while True:
    ret, frame = cap.read()
    if not ret:
        print("Sem sinal")
        break
    cv2.imshow("Camera", frame)
    if cv2.waitKey(1) == 27:
        break

cap.release()
cv2.destroyAllWindows()